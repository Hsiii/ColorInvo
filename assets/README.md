# Assets

Root-level release and marketing assets that are not app build inputs.

- `app-store/description.zh-Hant.md`: Traditional Chinese App Store listing copy.
- `app-store/screenshots`: App Store screenshots generated by `bun run ios:screenshots`.

The iOS asset catalog stays in `apps/ios/Resources` because Xcode consumes it when building the app.
